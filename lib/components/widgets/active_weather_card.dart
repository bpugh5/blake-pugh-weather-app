import 'package:flutter/material.dart';

class ActiveWeatherCard extends StatelessWidget {
  const ActiveWeatherCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card();
  }
}